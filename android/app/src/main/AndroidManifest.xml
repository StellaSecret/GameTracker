<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:label="GameTracker"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/LaunchTheme"
        android:hardwareAccelerated="true"
        android:windowSoftInputMode="adjustResize">

        <!-- Google Sign-In requires this activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Flutter embedding v2 -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>

        <!-- Google Mobile Ads SDK requires this App ID meta-data to be
             present or MobileAds.instance.initialize() (called from
             main.dart) throws at startup. Value comes from Gradle's
             manifestPlaceholders (see android/app/build.gradle), sourced
             from the ADMOB_APPLICATION_ID_ANDROID secret in CI, falling
             back to Google's public test App ID otherwise — same
             secret/fallback pattern as ADMOB_REWARDED_AD_UNIT_ANDROID in
             lib/services/ad_service.dart. These two MUST refer to the same
             AdMob "app" entry or ad requests fail with
             ERROR_CODE_INVALID_REQUEST. See SETUP.md. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobApplicationId}"/>

    </application>
</manifest>
